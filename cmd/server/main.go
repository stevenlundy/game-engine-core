// Package main is the entry point for the game-engine-core gRPC server.
// Full implementation is tracked in Phase 7 of TASKS.md.
package main

func main() {
	// TODO(phase7): read config from env vars (PORT, GAME_TYPE, HEADLESS, LOG_DIR),
	// register MatchmakingServer and GameSessionServer, enable gRPC reflection,
	// and handle SIGTERM/SIGINT for graceful shutdown.
}
