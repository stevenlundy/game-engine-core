# Binaries
bin/
/server
*.exe
*.exe~
*.dll
*.so
*.dylib

# Go build & test artifacts
*.test
*.out
coverage.html
coverage.txt

# Go workspace
go.work
go.work.sum

# Protobuf generated files are committed (api/proto/gen/ is tracked).
# Do NOT add api/proto/gen/ here — generated .pb.go files must be in the repo
# so that CI can run `go test ./...` without requiring protoc to be installed.

# Replay logs
*.glog
*.glog.gz

# Cook orchestration
.cook/

# IDE & OS
.DS_Store
.idea/
.vscode/
*.swp
*.swo
testserver
